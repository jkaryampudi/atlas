"""Deterministic reporting plane (Doc 04 §14, ADR-0012 consequence 4).

Pure DCP: injectable clock, committed tables in, committed tables + markdown
out. No agent import, no agent number — every figure is recomputable from the
ledger and vendor bars alone.
"""
